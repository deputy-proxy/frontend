# Altrum template replication

## Reference
https://altrum-template.webflow.io

## Replication ID
altrum-template

## Implementation
The canonical implementation is `implementation/index.html`. Tailwind CSS is the primary styling system and is compiled locally. Alpine.js provides the mobile navigation state; GSAP provides local reveal motion.

## External resources
No external JavaScript, CSS, API, iframe, embed, widget, or runtime application dependency is used. Visual treatment is implemented locally.

## Source capture
The source screenshot was captured with the repository Screenshot workflow after its Browserless preflight. Capture metadata reports a full-page 1920×21098 document. The source image is retained at `source/screenshot.png` and must remain the canonical visual reference.

## Iterations
| Iteration | Visual Verification | Main Findings |
|---|---|---|
| 001 | PENDING | First implementation capture pending automated source comparison. |

## Final verification
Pending until the implementation has been captured and directly compared with the canonical source screenshot.

## Deployment
Pending verified deployment.
