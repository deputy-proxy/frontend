import Alpine from "alpinejs";import {gsap} from "gsap";import {ScrollTrigger} from "gsap/ScrollTrigger";gsap.registerPlugin(ScrollTrigger);window.Alpine=Alpine;Alpine.start();window.addEventListener("load",()=>{gsap.utils.toArray("main section").forEach(s=>gsap.from(s.children,{opacity:0,y:14,duration:.65,stagger:.04,ease:"power2.out",scrollTrigger:{trigger:s,start:"top 88%",once:true}}))});