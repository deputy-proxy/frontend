name: Deploy frontend to GitHub Pages

on:
  push:
    branches: [main]
    paths-ignore:
      - 'replications/**/source/**'
      - 'replications/**/iterations/**'
      - 'replications/**/README.md'
  pull_request:
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v7
      - name: Setup Node
        uses: actions/setup-node@v7
        with:
          node-version: 24
      - name: Setup Pages
        uses: actions/configure-pages@v6
      - name: Install dependencies
        run: npm install
      - name: Build
        run: npm run build
      - name: Include replication artifacts
        run: |
          mkdir -p dist/replications
          cp -R replications/verdentix dist/replications/
          cp -R replications/investflowtemplate-webflow-io-home-pages-home-v3 dist/replications/
          cp -R replications/neurex dist/replications/
          mkdir -p dist/replications/kora
          cp -R replications/kora/source dist/replications/kora/
          cp -R replications/kora/iterations dist/replications/kora/
          cp replications/kora/README.md dist/replications/kora/
          mkdir -p dist/replications/thornhill
          cp -R replications/thornhill/source dist/replications/thornhill/
          cp -R replications/thornhill/iterations dist/replications/thornhill/
          cp -R replications/thornhill/implementation dist/replications/thornhill/
          cp replications/thornhill/README.md dist/replications/thornhill/
      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v5
        with:
          path: ./dist

  deploy:
    if: github.event_name != 'pull_request' && github.ref == 'refs/heads/main'
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5